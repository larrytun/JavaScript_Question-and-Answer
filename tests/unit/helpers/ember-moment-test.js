
import { emberMoment } from 'question-and-answer/helpers/ember-moment';
import { module, test } from 'qunit';

module('Unit | Helper | ember moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberMoment([42]);
  assert.ok(result);
});

